spring:
  datasource:
    username: root
    password: 8888
    url: jdbc:mysql://localhost:3306/movies
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernates:
        hibernate:
          format_sql: true

project:
  poster: poster/